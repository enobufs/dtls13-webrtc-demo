# docs/

Assets referenced by the top-level `README.md`.

- `screenshot.png` — the demo UI in a browser: the looped-back video with the
  "using DTLS v1.3" overlay, plus the on-page log (negotiated DTLS version,
  role, cipher). Capture it by running `./setup.sh && ./dtls13-webrtc-demo`,
  opening <http://localhost:8080>, clicking **Start**, and screenshotting once
  the video renders.
