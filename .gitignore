# --- Generated by setup.sh (reproducible; do not commit) ---

# Local pion checkouts that setup.sh clones and patches.
/forks/

# Compiled demo binary (built by `go build -o dtls13-webrtc-demo .`).
/dtls13-webrtc-demo

# go.sum is regenerated by setup.sh's `go mod tidy` against the local ./forks
# replaces, so it's not committed (it would reference the patched checkouts).
/go.sum

# --- Go build / test artifacts ---
*.test
*.out
*.prof
coverage.txt
coverage.html

# --- Editor / OS cruft ---
.DS_Store
*.swp
*~
.idea/
.vscode/

# --- Misc ---
*.log
